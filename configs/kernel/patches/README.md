# Kernel Patches

Out-of-tree patches applied on top of Flipper's kernel fork.
Applied in alphabetical order via `git am` or `quilt`.

Naming: `NNNN-short-description.patch`
Example: `0001-add-ostree-boot-support.patch`

Generate: `git format-patch -1 HEAD --output-directory /path/to/patches/`
